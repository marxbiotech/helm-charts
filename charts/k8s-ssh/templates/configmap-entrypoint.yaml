apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "k8s-ssh.fullname" . }}-entrypoint
  labels:
    {{- include "k8s-ssh.labels" . | nindent 4 }}
data:
  entrypoint.sh: |-
{{- .Files.Get "scripts/entrypoint.sh" | nindent 4 }}
